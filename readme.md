create a python vittual environment using:
```
python -m venv venv
```

activate the venv:
```
venv\scripts\activate
```

install the required dependencies using:
```
pip install -r requirements.txt
```

run the run.py to execute automate the execution of the other 4 files:
```
python run.py
```
